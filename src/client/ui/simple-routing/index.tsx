import { StaticRouter } from 'react-router-dom/server'
import { Routes, Route, Outlet } from 'react-router-dom'
import { useRouter } from 'next/router'

export { StaticRouter, Routes, Route, Outlet, useRouter }
